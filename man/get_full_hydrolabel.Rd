% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{get_full_hydrolabel}
\alias{get_full_hydrolabel}
\title{Get full hydrological variable label}
\usage{
get_full_hydrolabel(in_hydro_vars_dt, in_hydro_var)
}
\arguments{
\item{in_hydro_vars_dt}{data.table. Reference data.table containing hydrological variable
metadata with columns:
\itemize{
\item \code{hydro_var}: Variable names (e.g., "meanQ30past")
\item \code{hydro_var_root}: Root variable names without window suffixes (e.g., "meanQ")
\item \code{hydro_label}: Human-readable base labels (e.g., "Mean discharge")
\item \code{window_d}: Window duration for time-based variables
}}

\item{in_hydro_var}{character. The hydrological variable name to convert to a full label.
May include suffixes like "_30past", "_z", "_sqrt", "_log10", or "_samp".}
}
\value{
character. A human-readable label for the input variable. For variables with window
information, the label includes the window duration and time unit (days or years).
For transformed variables (e.g., scaled, log), the transformation suffix is removed before
label generation. Special cases like "basin_area_km2" and "meanQ3650past" have predefined labels.
}
\description{
Generates a human-readable label for hydrological variables based on their names and
a reference data.table containing variable metadata.
}
