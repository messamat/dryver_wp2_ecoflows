% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{read_biodt}
\alias{read_biodt}
\title{Read and clean biodiversity datasets}
\usage{
read_biodt(path_list, in_metadata_edna, in_miv_full_dt, include_bacteria = T)
}
\arguments{
\item{path_list}{Named list of file paths. Names should correspond to organism types
(e.g., "dia_sedi" for diatoms in sediment, "fun_biof" for fungi in biofilm).}

\item{in_metadata_edna}{data.table. Metadata containing eDNA sample information with columns:
\itemize{
\item \code{running_id}: Sample identifier
\item \code{sample_type}: Type of sample (e.g., "sediment", "biofilm")
\item \code{habitat}: Habitat type (e.g., "pool", "run")
\item \code{date}: Sampling date
\item \code{country}: Country name
}}

\item{in_miv_full_dt}{data.table. Macroinvertebrate dataset with taxonomic information.
Must contain column \verb{Genus / Higher taxonomic group}.}

\item{include_bacteria}{logical. If \code{FALSE}, bacteria are excluded from the output.
Default is TRUE.}
}
\value{
A named list of cleaned \code{data.table}s, one per organism group. The list includes:
\itemize{
\item Original datasets with standardized naming and formatting
\item \verb{_nopools} versions of datasets with pool samples removed
\item \code{miv_nopools_ept} and \code{miv_nopools_och} datasets with macroinvertebrates split by
taxonomic groups (EPT: Ephemeroptera, Plecoptera, Trichoptera; OCH: Odonata, Coleoptera,
Heteroptera)
}
}
\description{
Imports multiple biodiversity datasets (diatoms, fungi, macroinvertebrates, bacteria)
and harmonizes formats, dates, site/campaign IDs, and metadata.
}
\details{
The function performs the following cleaning steps:
\enumerate{
\item Reads all input files and assigns organism type
\item Standardizes  "Czech Republic" to "Czechia"
\item Converts date strings to Date objects
\item Creates \code{running_id} from site and campaign when needed
\item Removes specific taxa (Hydrachnidae, Nematoda, Nematomorpha) from macroinvertebrate datasets
\item Splits macroinvertebrates into EPT and OCH taxonomic groups
\item Fills missing sampling dates from eDNA metadata
\item Corrects known date errors (e.g., fungi 2012 → 2021)
\item Adds site and campaign information to bacterial datasets
\item Removes pool samples (creates \verb{_nopools} versions of datasets)
\item Optionally excludes bacterial datasets
}
}
