% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{scale_ssn_predictors}
\alias{scale_ssn_predictors}
\title{Scale predictors}
\usage{
scale_ssn_predictors(in_ssn, in_vars, scale_ssn_preds)
}
\arguments{
\item{in_ssn}{list. A list containing data with two named elements:
\itemize{
\item \code{obs}: A spatial data frame (sf object) containing observed predictors
\item \code{preds}: A list containing projected predictors (e.g., \code{preds_proj})
}}

\item{in_vars}{character. Vector of variable names to be scaled. Only variables present in
both \code{in_ssn$obs} and \code{in_vars} will be processed.}

\item{scale_ssn_preds}{logical. If TRUE, applies the same scaling to projected predictors.
Default is TRUE.}
}
\value{
list. The input list \code{in_ssn} with:
\itemize{
\item \code{obs}: Original observed data with additional scaled columns (suffix \verb{_z}) for each variable in \code{in_vars}.
\item \code{preds}: If \code{scale_ssn_preds} is TRUE, projected data with scaled columns (suffix \verb{_z})
for each variable in \code{in_vars} that exists in \code{preds$preds_proj}.
}
}
\description{
Standardizes (z-score normalization) predictors in both observed and projected data
using scaling parameters derived from the observed data.
}
\details{
The function performs z-score standardization
(subtracting the mean and dividing by the standard deviation)
on the specified variables. The scaling parameters (mean and standard deviation)
are computed from the observed data and then applied to both observed and
projected data to ensure consistency.

The scaled columns are named with a \verb{_z} suffix, and the original columns remain unchanged.
}
