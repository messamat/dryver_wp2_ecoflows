% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{compute_connectivity_proj}
\alias{compute_connectivity_proj}
\title{Compute connectivity metrics for projected hydrological data}
\usage{
compute_connectivity_proj(
  hydroproj_path,
  hydroref_path,
  in_drn_dt,
  network_ssnready_shp_list,
  subset_sites,
  in_sites_dt,
  STcon_window = 365,
  min_date = as.Date("1990-01-01")
)
}
\arguments{
\item{hydroproj_path}{character. Path to the projected hydrological data NetCDF file.}

\item{hydroref_path}{character. Path to the reference hydrological data NetCDF file.}

\item{in_drn_dt}{data.table. Drainage basin metadata with at least:
\itemize{
\item \code{catchment}: Catchment identifier
\item \code{country}: Country name
}}

\item{network_ssnready_shp_list}{list. Named list of river network shapefiles (sf objects)
indexed by country names.}

\item{subset_sites}{logical. If TRUE, results are subset to only include sites from \code{in_sites_dt}.}

\item{in_sites_dt}{data.table. Site information with at least:
\itemize{
\item \code{reach_id}: River reach identifier
\item \code{site}: Site identifier
}}

\item{STcon_window}{integer. Time window (in days) for computing rolling STcon metrics.
Default is 365 (1 year).}

\item{min_date}{Date. Minimum date to consider in the analysis. Default is 1990-01-01.}
}
\value{
data.table. A combined data.table with connectivity metrics including:
\itemize{
\item \code{STcon_directed_mean_yr}: Directed spatial-temporal connectivity (mean per year)
\item \code{STcon_undirected_mean_yr}: Undirected spatial-temporal connectivity (mean per year)
\item \code{Fdist_undmean_yr}: Functional distance (mean per year)
\item Metadata columns: \code{UID}, \code{site}, \code{cat}, \code{date}, \code{year}, \code{country}, \code{gcm}, \code{scenario}
}
}
\description{
Calculates spatial-temporal connectivity (STcon) and functional distance (Fdist) metrics
for a given hydrological projection, using both directed and undirected network approaches.
}
\details{
The function performs the following steps:
\enumerate{
\item Imports and formats hydrological projection data using \code{import_hydromod_gcm}
\item Prepares data for connectivity analysis using \code{prepare_data_for_STcon}
\item Computes directed STcon metrics for each year using a rolling window approach
\item Post-processes the directed STcon results
\item Optionally subsets results to specific sites
\item Computes undirected STcon metrics similarly
\item Computes functional distance (Fdist) metrics
\item Combines all metrics into a single output data.table
}

The STcon metrics are computed using:
\itemize{
\item Directed connectivity: considers flow direction in the network
\item Undirected connectivity: ignores flow direction
}

The Fdist metric represents the functional distance between sites based on their
hydrological status and network structure.
}
