% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{get_ssn_emtrends}
\alias{get_ssn_emtrends}
\title{Compute estimated marginal trends for SSN models}
\usage{
get_ssn_emtrends(
  in_mod,
  in_pred_var,
  in_pred_var_label,
  interaction_var,
  in_drn_dt,
  in_emtrends_dt = NULL,
  plot = T
)
}
\arguments{
\item{in_mod}{model. A fitted SSN model object containing:
\itemize{
\item \code{ssn.object}: List with \code{obs} data.frame containing the predictor variable
\item \code{formula}: Model formula to extract the response variable
}}

\item{in_pred_var}{character. Name of the predictor variable for which to compute EM trends.}

\item{in_pred_var_label}{character. Label for the predictor variable (currently not used in plotting).}

\item{interaction_var}{character. Name of the categorical variable to condition the EM trends on.}

\item{in_drn_dt}{data.table. Data.table containing drainage basin metadata with at least:
\itemize{
\item \code{country}: Country names
\item Other columns to merge with the EM trends results
}}

\item{in_emtrends_dt}{data.table. Optional pre-computed EM trends data.table. If provided,
this will be used instead of computing new EM trends.}

\item{plot}{logical. If TRUE (default), generates a ggplot of the EM trends with confidence intervals.}
}
\value{
list. A list containing:
\itemize{
\item \code{dt}: data.table with EM trends results including columns:
\code{interaction_var}, \code{trend}, \code{p_value}, \code{response_var}, and additional metadata
from \code{in_drn_dt}
\item \code{plot}: ggplot object if \code{plot=TRUE}, otherwise NULL
}
}
\description{
Calculates estimated marginal trends (EM trends) for a given predictor variable across levels
of an interaction variable, with optional plotting functionality.
}
\details{
The function computes estimated marginal trends (EM trends) for a predictor variable across levels
of an interaction variable. If \code{in_emtrends_dt} is not provided, it:
\enumerate{
\item Extracts the response variable name from the model formula
\item Computes EM trends for the predictor variable by levels of the interaction variable
\item Extracts p-values from the trend tests
\item Merges with drainage basin metadata
\item Orders countries in a specific sequence (Finland, France, Hungary, Czechia, Croatia, Spain)
}

The resulting plot shows:
\itemize{
\item Points with error bars representing the trend estimates and confidence intervals
\item A horizontal reference line at y=0
\item Customizable axis labels
}
}
