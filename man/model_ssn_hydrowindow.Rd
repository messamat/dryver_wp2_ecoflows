% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{model_ssn_hydrowindow}
\alias{model_ssn_hydrowindow}
\title{Model SSN with hydrological variables across time windows}
\usage{
model_ssn_hydrowindow(
  in_ssn,
  organism,
  formula_root,
  hydro_var,
  response_var,
  ssn_covtypes,
  partition_formula = as.formula("~ as.factor(campaign)"),
  random_formula = as.formula("~ country"),
  family = "Gaussian",
  estmethod = "ml",
  standardize_hydro_var = T,
  test_parabolic = F,
  include_state_of_flow = F,
  include_seasonality = F
)
}
\arguments{
\item{in_ssn}{A named list of SSN objects, one for each organism group.
Each element should contain an \code{ssn} component with the SSN object.}

\item{organism}{A character string specifying which organism's SSN object to use from \code{in_ssn}.}

\item{formula_root}{A character string of fixed effects to be included in all models.}

\item{hydro_var}{A character string specifying the hydrological variable to test.
If NULL, only the fixed effects in \code{formula_root} are used.}

\item{response_var}{A character string specifying the biological response variable.}

\item{ssn_covtypes}{A data.table specifying the covariance types for model fitting.
Must contain columns: \code{down}, \code{up}, \code{euc}, and \code{label}.}

\item{partition_formula}{A formula object for a partition factor (e.g., sampling campaign).
Default is \code{~ as.factor(campaign)}.}

\item{random_formula}{A formula object for random effects (e.g., country).
Default is \code{~ country}.}

\item{family}{A character string for the \code{glm} family, or 'Gaussian' for \code{lm}.
Default is "Gaussian".}

\item{estmethod}{A character string for the estimation method (e.g., 'ml' or 'reml').
Default is 'ml'.}

\item{standardize_hydro_var}{logical. If TRUE (default), standardizes the hydrological variable
before including it in the model. The standardized variable is added to the SSN object with
a '_scaled' suffix.}

\item{test_parabolic}{logical. If TRUE, includes a parabolic term for the hydrological variable
in the model formula. Default is FALSE.}

\item{include_state_of_flow}{logical. If TRUE, includes the \code{state_of_flow} variable in the
model formula. Default is FALSE.}

\item{include_seasonality}{logical. If TRUE, includes day of year (\code{doy}) and its square in the
model formula to account for seasonality. Default is FALSE.}
}
\value{
A list containing:
\itemize{
\item \code{ssn_list}: A named list of fitted SSN model objects (or failure objects)
\item \code{ssn_glance}: A data.table of model summary statistics with columns:
\itemize{
\item \code{covtypes}: Covariance type combination label
\item \code{fit_status}: "ok" or "failed"
\item \code{error_message}: Error message for failed models (NA for successful models)
\item \code{AICc}: Corrected Akaike Information Criterion
\item Additional model metrics from \code{SSN2::glance}
\item Metadata: \code{organism}, \code{response_var}, \code{hydro_var}, \code{family}
}
}
}
\description{
Automates the process of fitting Spatial Stream Network (SSN) models to test the effect
of a specific hydrological variable on a biological response variable, collecting model
summaries for comparison.
}
\details{
The function:
\enumerate{
\item Optionally standardizes the hydrological variable
\item Constructs the full model formula based on the provided components:
\itemize{
\item Main effect of hydrological variable
\item Interaction between hydrological variable and country
\item Fixed effects from \code{formula_root}
\item Optional parabolic term for hydrological variable
\item Optional seasonality terms (doy and doy^2)
\item Optional state_of_flow term
}
\item Fits SSN models for each covariance type combination using \code{quick_ssn}
\item Collects model summary statistics using \code{SSN2::glance}
\item Returns both the fitted models and their summaries
}
}
