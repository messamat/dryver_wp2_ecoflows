% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{snap_points_inner}
\alias{snap_points_inner}
\title{Snap points to the nearest target geometry}
\usage{
snap_points_inner(in_pts, in_target, sites_idcol, attri_to_join = NULL)
}
\arguments{
\item{in_pts}{A \code{SpatVector} of points (from \code{terra}).}

\item{in_target}{A \code{SpatVector} target geometry (e.g., lines).}

\item{sites_idcol}{Name of the column containing point IDs (used to filter duplicates).}

\item{attri_to_join}{Optional. Either a character vector of column names from
\code{in_target} to join, or \code{"all"} to join all attributes. Default: NULL
(i.e. no attribute is joined)}
}
\value{
A \code{SpatVector} of snapped points with optional joined attributes.
}
\description{
Snaps input points to the nearest target features (e.g., lines or polygons),
creating snapped points with optional attribute joins.
}
\details{
\itemize{
\item Snapping is performed using \code{terra::nearest} with line output, then
converted to endpoints.
\item If \code{attri_to_join} is provided, attributes of the nearest target are joined.
}
}
\note{
Uses a heuristic: filtering duplicates with \code{duplicated(values(.)[, sites_idcol])}.
This may drop unintended points if IDs are not unique.
}
