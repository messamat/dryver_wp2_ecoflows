% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{get_hydrowindow_emtrends}
\alias{get_hydrowindow_emtrends}
\title{Extract and plot marginal slopes (EMTrends) from best models}
\usage{
get_hydrowindow_emtrends(perf_dt, in_hydro_vars_dt, in_drn_dt, plot)
}
\arguments{
\item{perf_dt}{data.table. A data.table (typically the \code{best} element) returned by
\code{prepare_hydrowindow_perf_table()}. Must contain columns:
\itemize{
\item \code{hydro_var}: Hydrological variable name
\item \code{mod}: List column containing fitted SSN model objects
\item \code{test_parabolic}: Logical indicating if parabolic term was tested
}}

\item{in_hydro_vars_dt}{data.table. Metadata table with hydrological variable information,
containing at least \code{hydro_var}, \code{hydro_var_root}, and \code{hydro_label}.}

\item{in_drn_dt}{data.table. Drainage basin metadata with at least:
\itemize{
\item \code{country}: Country name
\item Other columns needed for \code{get_ssn_emtrends}
}}

\item{plot}{logical. If TRUE, generates a faceted plot of the estimated slopes.}
}
\value{
list. A list containing:
\itemize{
\item \code{dt}: data.table with estimated marginal trends for all predictors
\item \code{plot}: ggplot object if \code{plot=TRUE}, otherwise NULL
}
}
\description{
Runs \code{get_ssn_emtrends()} for all predictors in the best models to compute and visualize
estimated marginal trends (slopes) for hydrological variables.
}
\details{
The function:
\enumerate{
\item For each hydrological variable in \code{perf_dt}:
\itemize{
\item Extracts the corresponding model
\item Handles special case for null models
\item Computes estimated marginal trends using \code{get_ssn_emtrends}
\item Records whether parabolic terms were tested
}
\item Combines all results into a single data.table
\item Adds human-readable labels and classification information
\item Caps confidence interval bounds at the observed range of trend values
\item Optionally creates a faceted plot of the estimated slopes
data.table` and plots of estimated slopes across predictors.
}
}
