% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{plot_formula_emtrends}
\alias{plot_formula_emtrends}
\title{Plot estimated marginal trends from formula}
\usage{
plot_formula_emtrends(
  in_mod_fit,
  in_drn_dt,
  in_hydro_vars_dt,
  plot = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{in_mod_fit}{model. A fitted model object (typically from ssn models) containing:
\itemize{
\item \code{formula}: Model formula
\item \code{ssn.object}: List with \code{obs} data.frame containing the predictor variables
}}

\item{in_drn_dt}{data.table. Data.table containing drainage basin metadata with at least:
\itemize{
\item \code{country}: Country names
\item Other columns that may be used for grouping and coloring in the plot
}}

\item{in_hydro_vars_dt}{data.table. Reference data.table containing hydrological variable metadata
with columns: \code{hydro_var}, \code{hydro_var_root}, \code{hydro_label}, and \code{window_d}.}

\item{plot}{logical. If TRUE (default), generates a summary plot of the EM trends.}

\item{verbose}{logical. If TRUE (default), prints messages about processing progress and skipped terms.}
}
\value{
list. A list containing:
\itemize{
\item \code{dt}: data.table with EM trends results for all terms, including columns:
\code{trend}, confidence intervals, \code{p_value}, \code{response_var}, \code{pred_var_name},
\code{source_term}, \code{predictor}, \code{spec_factor}, and \code{pred_var_label}
\item \code{plot}: ggplot object if \code{plot=TRUE}, otherwise NULL
\item \code{skipped}: list of terms that were skipped with reasons
}
}
\description{
Computes and visualizes estimated marginal trends (EM trends) for each term
in a model formula, handling both continuous predictors and interaction terms.
}
\details{
The function processes each term in the model formula to:
\enumerate{
\item Identify continuous predictors and factor variables
\item Compute EM trends for each continuous predictor
\item Handle interaction terms by conditioning on factor variables
\item Format variable names using \code{get_full_hydrolabel}
\item Create a summary plot showing trend estimates with confidence intervals
}
}
