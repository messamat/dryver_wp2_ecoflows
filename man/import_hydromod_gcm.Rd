% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{import_hydromod_gcm}
\alias{import_hydromod_gcm}
\title{Import hydrological model data from GCM projections}
\usage{
import_hydromod_gcm(hydroproj_path, hydroref_path, in_drn_dt)
}
\arguments{
\item{hydroproj_path}{character. File path to the projected hydrological NetCDF file.
The filename is expected to follow a specific naming convention containing metadata
(e.g., catchment, variable name, time period, GCM, scenario, date range).}

\item{hydroref_path}{character. File path to the reference hydrological NetCDF file.}

\item{in_drn_dt}{data.table. Data.table containing drainage basin information with columns:
\itemize{
\item \code{catchment}: Name of the catchment
\item \code{country}: Country associated with the catchment
}}
}
\value{
list. A list containing two elements:
\itemize{
\item \code{metadata_dt}: data.table with extracted metadata from the filename, including:
\code{catchment}, \code{varname}, \code{time_period}, \code{gcm}, \code{scenario}, \code{date_range}, \code{file_end}, \code{country}, and \code{path}.
\item \code{hydro_dt}: data.table combining reference and projected hydrological data with columns:
\code{reach_id}, \code{date}, \code{isflowing} (renamed from \code{flowstate}), and \code{qsim} (renamed from \code{discharge}).
}
}
\description{
Imports and combines reference and projected hydrological data from NetCDF files,
including metadata extraction and formatting for downstream analysis.
}
\details{
The function first extracts metadata from the projected file's name by splitting the basename
on underscores. It then reads both the reference and projected NetCDF files to obtain:
\itemize{
\item Reach IDs
\item Dates (converted from NetCDF format to R Date objects)
\item The specified hydrological variable (e.g., flow state or discharge)
}

The reference and projected datasets are combined by:
\itemize{
\item Taking all reference data before the minimum date in the projected data
\item Taking all projected data after the maximum date in the reference data
This ensures no overlap between the two periods.
}

Special handling is applied for the "Lepsamanjoki" catchment, which is renamed to
"Lepsamaanjoki" for consistency.
}
