% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{plot_hydrocon_histproj}
\alias{plot_hydrocon_histproj}
\title{Plot hydrological data from historical and future periodes}
\usage{
plot_hydrocon_histproj(
  in_hydrocon_proj,
  in_hydromod_comb_hist,
  in_ssn_eu_summarized,
  in_drn_dt,
  year_smooth = 3,
  outdir
)
}
\arguments{
\item{in_hydrocon_proj}{list. A list of data.tables containing projected hydrological metrics
with columns: \code{reach_id}, \code{country}, \code{gcm}, \code{scenario}, \code{year}, \code{DurD_yr}, etc.}

\item{in_hydromod_comb_hist}{list. A list of historical hydrological model data.tables with
columns: \code{reach_id}, \code{date}, \code{isflowing}, etc.}

\item{in_ssn_eu_summarized}{list. A list containing SSN objects with river network information.
The first element should contain \verb{$ssn$edges} with columns: \code{cat}, \code{country}, \code{length_m}.}

\item{in_drn_dt}{data.table. Drainage basin metadata with at least:
\itemize{
\item \code{country}: Country name
\item Other columns for merging
}}

\item{year_smooth}{integer. Number of years for smoothing window. Default is 3.}

\item{outdir}{character. Output directory path for saving plots.}
}
\value{
list. A list containing ggplot objects:
\itemize{
\item \code{DurD_wmean_hist}: Historical length-weighted annual percentage of dry days
\item \code{per_npr_hist}: Historical percent non-perennial network length
\item \code{DurD_wmean_smooth_proj}: Projected length-weighted annual percentage of dry days
\item \code{per_npr_smooth_proj}: Projected percent non-perennial network length
}
}
\description{
Creates time series plots comparing historical and projected hydrological metrics
(dry day proportion and non-perennial network length) across countries and GCMs.
}
\details{
The function creates several types of plots:

Historical plots (1970-2021):
\itemize{
\item Time series of length-weighted annual percentage of dry days
\item Time series of percent non-perennial network length
\item Both include reference points from sampling period (2021)
}

Projected plots (1990-2100):
\itemize{
\item Time series of length-weighted annual percentage of dry days (smoothed)
\item Time series of percent non-perennial network length (smoothed)
\item Both show median and min-max range across GCMs
\item Both include vertical line at 2021 to separate historical and future periods
}
}
