% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{test_country_emtrends}
\alias{test_country_emtrends}
\title{Test for country-level differences in trends}
\usage{
test_country_emtrends(emtrends_dt, permutations_dt, out_dir)
}
\arguments{
\item{emtrends_dt}{A data.table containing hydrological trend estimates with required columns:
\itemize{
\item \code{organism}: Organism identifier (e.g., "bac_biof_nopools")
\item \code{hydro_var}: Hydrological variable name (e.g., "DurD365past_scaled")
\item \code{test_parabolic}: Logical indicating if parabolic test was used (TRUE/FALSE)
\item \code{hydro_var_root}: Root hydrological variable (e.g., "DurD", "FreD")
\item \code{country}: Country name (must include all 6: Finland, Hungary, France, Czechia, Croatia, Spain)
\item \code{trend}: Estimated trend coefficient
\item \code{SE}: Standard error of the trend estimate
\item \code{hydro_class}: Hydrological class (e.g., "Drying duration")
\item \code{organism_class}: Organism class (e.g., "Bacteria", "Macroinvertebrates")
\item \code{organism_label}: Human-readable organism label
\item \code{organism_sub}: Substrate type ("Biof." or "Sedi.")
\item \code{hydro_label}: Human-readable hydrological variable label
}}

\item{dt}{data.table with columns: country, drn_trend_rank_boot, drn_trend_rank_null, boot_i}

\item{group_col}{Character vector of grouping variables (NULL for global test)}
}
\value{
A list containing:
\itemize{
\item \code{heterogeneity_dt}: Data.table with heterogeneity test results (Q, Qp, tau², I²)
\item \code{heterogeneity_plot}: ggplot object visualizing heterogeneity across organisms/models
\item \code{kendallw_dt}: Data.table with Kendall's W test results for rank consistency
\item \code{rank_test_dt}: Data.table with bootstrap test results for hypothesized order fit
}

data.table with pairwise comparisons and significance
}
\description{
Performs three complementary analyses to evaluate country-specific biodiversity
responses to hydrological predictors:
\enumerate{
\item \strong{Heterogeneity Test}: Assesses whether slope estimates differ significantly across countries
for each organism × hydrological variable combination using random-effects meta-analysis.
\item \strong{Rank Consistency Test}: Evaluates whether country rankings are consistent across
hydrological models using Kendall's W coefficient of concordance.
\item \strong{Hypothesis Test}: Tests whether observed country rankings match the hypothesized order
(Finland > Hungary > France > Czechia > Croatia > Spain) using a bootstrap + permutation approach.
}
}
\details{
\subsection{Statistical Methods}{

\strong{1. Heterogeneity Test (Random-Effects Meta-Analysis)}
\itemize{
\item Uses \code{metafor::rma()} with REML estimation to test if country-level slope estimates
differ significantly within each organism × hydro_var × test_parabolic group.
\item \strong{Q-test}: Tests for heterogeneity (Qp < 0.05 indicates significant differences across countries).
\item \strong{I²}: Quantifies the percentage of variance due to between-country heterogeneity (0-100\%).
\itemize{
\item I² < 25\%: Low heterogeneity (consistent across countries)
\item I² > 50\%: High heterogeneity (substantial country-specific differences)
}
\item Groups with < 2 countries or invalid SEs return NA.
}

\strong{2. Rank Consistency Test (Kendall's W)}
\itemize{
\item Tests whether country rankings are consistent across hydrological models (hydro_var_root).
\item Uses \code{vegan::kendall.global()} with 999 permutations and Holm p-value adjustment.
\item Only applied to drying variables (excludes flow magnitude: 'oQ10', 'maxPQ').
\item For variables where higher values indicate stronger drying effects (FstDrE, STcon),
trend signs are reversed to ensure consistent ranking direction.
}

\strong{3. Hypothesis Test (Bootstrap + Permutation)}
\itemize{
\item Tests if observed country rankings match the hypothesized order:
\strong{Finland > Hungary > France > Czechia > Croatia > Spain}
(1 = most negative trends, 6 = lest negative trend).
\item For each organism × hydro_var_root group:
\enumerate{
\item Draws 1000 bootstrap samples from N(μ = trend_rel, σ = SE_rel)
\item Ranks the bootstrap estimates across countries
\item Computes Spearman's ρ between bootstrap ranks and hypothesized ranks
\item Generates null distribution by permuting ranks within each bootstrap iteration
\item Calculates p-value as proportion of null correlations where |ρ_null| ≥ |ρ_obs|
}
}
}
}
