% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{fix_confluences_inner}
\alias{fix_confluences_inner}
\title{Fix complex confluences in stream networks}
\usage{
fix_confluences_inner(
  shp,
  from = "FROM_NODE",
  to = "TO_NODE",
  step = 10,
  fields_to_keep = NULL
)
}
\arguments{
\item{shp}{A \code{SpatialLinesDataFrame} with \code{FROM_NODE} and \code{TO_NODE} fields.}

\item{from}{Column name of upstream node IDs (default \code{"FROM_NODE"}).}

\item{to}{Column name of downstream node IDs (default \code{"TO_NODE"}).}

\item{step}{Numeric. Distance (in map units) by which streams are adjusted downstream.}

\item{fields_to_keep}{Optional. Column names to keep from the original attributes.}
}
\value{
A corrected \code{SpatialLinesDataFrame} with confluences fixed.
}
\description{
Adjusts complex stream confluences (nodes with >2 incoming streams) in a
\code{SpatialLinesDataFrame}. Outermost streams' end vertices are shifted
downstream by a given step distance, new nodes are created,
and affected lines are split.
}
\details{
\itemize{
\item The function assumes the network is otherwise topologically correct.
\item No input validation is performed; invalid inputs may cause errors.
}
}
\examples{
\dontrun{
rios <- rgdal::readOGR("streams_Pt.shp", "streams_Pt")
corrected <- fix_confluences_inner(rios, step=10)
rgdal::writeOGR(corrected, "streams_corrected.shp", "streams_corrected", "ESRI Shapefile")
}

 @export
}
\references{
Based on: \url{https://github.com/miguel-porto/fix-streams}
}
