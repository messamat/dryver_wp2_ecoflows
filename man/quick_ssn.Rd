% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{quick_ssn}
\alias{quick_ssn}
\title{Quick SSN model fitting}
\usage{
quick_ssn(
  in_ssn,
  in_formula,
  ssn_covtypes,
  partition_formula = as.formula("~ as.factor(campaign)"),
  random_formula = as.formula("~ country"),
  family = "Gaussian",
  estmethod = "ml"
)
}
\arguments{
\item{in_ssn}{A spatial stream network (SSN) object created by the SSN2 package.}

\item{in_formula}{A formula object or character string specifying the model formula.
For example: "y ~ x1 + x2"}

\item{ssn_covtypes}{A data.table specifying the covariance types for the SSN model components.
Must contain columns:
\itemize{
\item \code{down}: Character vector of tail-down covariance types to test
\item \code{up}: Character vector of tail-up covariance types to test
\item \code{euc}: Character vector of Euclidean covariance types to test
\item \code{label}: Character vector of labels for each combination (used to name output list elements)
}}

\item{partition_formula}{A formula object for a partition factor (e.g., sampling campaign).
Default is \code{~ as.factor(campaign)}.}

\item{random_formula}{A formula object for random effects (e.g., country).
Default is \code{~ country}.}

\item{family}{A string or family object for the \code{glm} family. Use "Gaussian" for \code{lm}.
Default is "Gaussian".}

\item{estmethod}{A character string for the estimation method (e.g., 'ml' for Maximum Likelihood,
'reml' for Restricted Maximum Likelihood). Default is 'ml'.}
}
\value{
A named list of fitted SSN model objects (either \code{ssn_lm} or \code{ssn_glm} objects),
or objects indicating failure for combinations that did not converge.
Failed models contain:
\itemize{
\item \code{fit_status}: "failed"
\item \code{error_message}: The error message
\item \code{label}: The model label
\item \code{taildown}, \code{tailup}, \code{euc}: The covariance types used
}
}
\description{
A wrapper function to fit linear or generalized linear spatial stream network (SSN) models
with different covariance structures.
}
\details{
The function:
\enumerate{
\item Creates distance matrices for the SSN object using \code{SSN2::ssn_create_distmat}
\item Fits models for each combination of covariance structures specified in \code{ssn_covtypes}
\item For Gaussian family, uses \code{ssn_lm}; for other families, uses \code{ssn_glm}
\item Returns a named list of model objects, with one element for each covariance combination
}
}
