% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{compute_Fdist}
\alias{compute_Fdist}
\title{Calculate distance to nearest active site}
\usage{
compute_Fdist(
  sites_status_matrix,
  network_structure,
  routing_mode,
  raw_dist_matrix,
  in_net_shp_path
)
}
\arguments{
\item{sites_status_matrix}{matrix. A matrix representing the status of each site (active/inactive).
The matrix should have:
\itemize{
\item Columns: One for each site
\item Rows: One for each monitored time step (e.g., day)
\item Values: 1 for active/wet, 0 for inactive/dry
\item Warning: No other columns should be included (e.g., date, other IDs)
}}

\item{network_structure}{matrix. A square adjacency matrix representing connections among sites:
\itemize{
\item For a given site (row), each adjacent connected site (column) is given a value of 1, others 0
\item Must have the same number of rows and columns as there are columns in \code{sites_status_matrix}
}}

\item{routing_mode}{character. The direction for graph connectivity when directed:
\itemize{
\item "in": Routing from upstream (default for river networks)
\item "out": Routing from downstream
\item "all": Consider all connections regardless of direction
}}

\item{raw_dist_matrix}{matrix. A distance matrix representing distances between sites.
Can be any type of distance (Euclidean, environmental, topographic, etc.) between pairs of sites.
Must have the same dimensions as \code{network_structure}.}

\item{in_net_shp_path}{character or sf object. Path to the river network shapefile or an sf object
containing the network data with columns:
\itemize{
\item \code{from}: From node identifier
\item \code{to}: To node identifier
\item \code{to_cat_shp}: Downstream connection identifier (NA for outlets)
\item \code{UID}: Unique identifier for each site
}}
}
\value{
data.table. A data.table with columns:
\itemize{
\item \code{date}: Date of the observation
\item \code{ID}: Site identifier (integer)
\item \code{Fdist}: Distance to the nearest active site
\item \code{UID}: Unique identifier for each site
}
}
\description{
Computes the distance to the nearest active site based on spatiotemporal graph networks.
The function quantifies connectivity distances where each cell value represents a feature
defining connectivity "on" or "off".
}
