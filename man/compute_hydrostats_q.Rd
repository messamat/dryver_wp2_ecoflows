% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{compute_hydrostats_q}
\alias{compute_hydrostats_q}
\title{Compute flow magnitude statistics}
\usage{
compute_hydrostats_q(in_hydromod_dt = hydromod_dt_sites)
}
\arguments{
\item{in_hydromod_dt}{data.table. Input data.table containing hydrological model output.
Must include columns: \code{reach_id}, \code{date}, \code{doy} (day of year), and \code{qsim} (simulated flow).
Defaults to \code{hydromod_dt_sites} if not provided.}
}
\value{
data.table. The input data.table with additional columns for:
\itemize{
\item Rolling mean flow: \verb{meanQ[10,30,60,90,120,180,365,1825,3650]past}
\item Flow percentile: \code{Pqsim} (percentile of each flow value relative to the full flow duration curve)
\item Proportion of days below 10th percentile: \verb{uQ90_[10,30,60,90,120,180,365]past}
\item Proportion of days above 90th percentile: \verb{oQ10_[10,30,60,90,120,180,365]past}
\item Maximum flow percentile in rolling windows: \verb{maxPQ_[3,10,30,60,90,120,180]past}
\item Percentiles of mean flow within past time windows (grouped by day of year for shorter windows,
or by reach for longer windows)
}
}
\description{
Calculates a set of rolling hydrological statistics for simulated streamflow data,
including mean flow over multiple time windows, flow percentiles, and proportions of days
below/above threshold percentiles.
}
\details{
The function first orders the data by \code{reach_id} and \code{date}. It then computes:
\enumerate{
\item Rolling mean flow over 9 different time windows (10 to 3650 days)
\item Flow percentiles relative to the entire flow duration curve (FDC) for each reach
\item Proportion of days below the 10th percentile (uQ90) and above the 90th percentile (oQ10)
over 7 time windows (10 to 365 days)
\item Maximum flow percentile over 8 time windows (3 to 365 days)
\item Percentiles of mean flow within past time windows, computed either by day of year (for windows
up to 180 days) or for the entire record (for windows of 365 days or more)
}
}
\examples{
hydromod_dt <- data.table(
  reach_id = rep(1001, 365 * 10),
  date = seq(as.Date("2014-01-01"), as.Date("2023-12-31"), by = "day"),
  doy = rep(1:365, 10),
  qsim = rnorm(3650, mean = 100, sd = 20)
)
compute_hydrostats_q(hydromod_dt)

}
