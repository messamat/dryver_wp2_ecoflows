% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{impute_hydromod}
\alias{impute_hydromod}
\title{Impute hydrological model data for unmodeled network reaches}
\usage{
impute_hydromod(
  in_network_path,
  varname,
  in_hydromod_drn,
  in_network_idcol = "cat_cor"
)
}
\arguments{
\item{in_network_path}{character. Path to the river network shapefile or vector file.}

\item{varname}{character. The variable name to impute, either 'isflowing' (flow status)
or 'qsim' (simulated discharge).}

\item{in_hydromod_drn}{list. A list containing hydrological model data with two elements:
\itemize{
\item \code{data_all}: data.table with columns \code{reach_id}, \code{date}, and the variable to impute
(\code{isflowing} or \code{qsim}). May also contain topological columns like \code{to_reach_hydromod}
and \code{upstream_area_net}.
\item \code{dates_format}: data.table with date formatting information including columns:
\code{date}, \code{month}, \code{hy}, \code{doy}
}}

\item{in_network_idcol}{character. The column name in the network shapefile that contains
the reach identifiers. Default is 'cat_cor'.}
}
\value{
data.table. A combined data.table containing:
\itemize{
\item The original modeled data
\item Imputed data for unmodeled reaches
\item Additional date formatting columns (\code{month}, \code{hy}, \code{doy})
}
}
\description{
Imputes hydrological data (flow status or discharge) for river network reaches
that were not explicitly modeled by using data from the largest upstream segment.
}
\details{
The function handles two types of variables differently:

For \code{isflowing} (flow status):
\itemize{
\item For each unmodeled reach, finds the largest upstream reach that was modeled
\item Imputes the flow status from this upstream reach
}

For \code{qsim} (discharge):
\itemize{
\item For each unmodeled reach, sums the discharge from all upstream reaches
that were modeled and share the same date
}
}
