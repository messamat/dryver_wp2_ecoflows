% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{get_ssn_emmeans}
\alias{get_ssn_emmeans}
\title{Compute estimated marginal means for SSN models}
\usage{
get_ssn_emmeans(
  in_mod,
  in_pred_var,
  interaction_var,
  in_drn_dt,
  in_pred_var_label = NULL,
  in_emm_dt = NULL,
  plot = T,
  label_pred_var = T
)
}
\arguments{
\item{in_mod}{model. A fitted SSN model object containing:
\itemize{
\item \code{ssn.object}: List with \code{obs} data.frame containing the predictor variable
\item \code{formula}: Model formula to extract the response variable
}}

\item{in_pred_var}{character. Name of the predictor variable for which to compute EMMs.}

\item{interaction_var}{character. Name of the categorical variable to condition the EMMs on.}

\item{in_drn_dt}{data.table. Data.table containing drainage basin metadata with at least:
\itemize{
\item \code{country}: Country names
\item Other columns to merge with the EMM results
}}

\item{in_pred_var_label}{character. Optional label for the predictor variable axis.
If provided and \code{label_pred_var} is TRUE, this will be used as the x-axis label.}

\item{in_emm_dt}{data.table. Optional pre-computed EMM data.table. If provided, this will
be used instead of computing new EMMs. Must contain columns: \code{pred_var}, \code{interaction_var},
\code{emmean}, \code{asymp.LCL}, \code{asymp.UCL}, and \code{response_var}.}

\item{plot}{logical. If TRUE (default), generates a ggplot of the EMMs with confidence ribbons.}

\item{label_pred_var}{logical. If TRUE (default) and \code{in_pred_var_label} is provided,
uses the custom label for the x-axis.}
}
\value{
list. A list containing:
\itemize{
\item \code{dt}: data.table with EMM results including columns:
\code{pred_var}, \code{interaction_var}, \code{emmean}, \code{asymp.LCL}, \code{asymp.UCL}, \code{response_var},
and additional metadata from \code{in_drn_dt}
\item \code{plot}: ggplot object if \code{plot=TRUE}, otherwise NULL
}
}
\description{
Calculates estimated marginal means (EMMs) for a given predictor variable across levels
of an interaction variable, with optional plotting functionality.
}
\details{
The function computes estimated marginal means (EMMs) for a predictor variable across levels
of an interaction variable. If \code{in_emm_dt} is not provided, it:
\enumerate{
\item Extracts the observation data from the model's ssn.object
\item Creates a sequence of 50 values spanning the range of the predictor variable
\item Computes EMMs at these values for each level of the interaction variable
\item Merges with drainage basin metadata
\item Orders countries in a specific sequence (Finland, France, Hungary, Czechia, Croatia, Spain)
}

The resulting plot shows:
\itemize{
\item Lines for each level of the interaction variable
\item Confidence ribbons (default 95\% CIs)
\item Customizable axis labels
}
}
