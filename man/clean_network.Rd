% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{clean_network}
\alias{clean_network}
\title{Clean river network}
\usage{
clean_network(
  rivnet_path,
  idcol,
  node_clustering_dist,
  min_segment_length = 20,
  outdir = NULL,
  save_gpkg = FALSE,
  return_path = FALSE
)
}
\arguments{
\item{rivnet_path}{Path to input river network file (GeoPackage).}

\item{idcol}{Column name for segment IDs.}

\item{node_clustering_dist}{Numeric; distance threshold for clustering
nearby nodes (in CRS units, e.g. meters).}

\item{min_segment_length}{Minimum length (in CRS units) for dangling
segments to be retained.}

\item{outdir}{Optional output directory.}

\item{save_gpkg}{Logical; whether to save cleaned network as GeoPackage.}

\item{return_path}{Logical; if TRUE, return file path instead of sf object.}
}
\value{
Cleaned river network as an \code{sf} object or file path (if \code{return_path=TRUE}).
}
\description{
Cleans a river network to ensure topological consistency, remove spurious
segments, and cluster nearby nodes. Steps include: splitting at confluences,
clustering nodes within a distance threshold, removing short dangling
segments, and re-assigning original IDs.
}
