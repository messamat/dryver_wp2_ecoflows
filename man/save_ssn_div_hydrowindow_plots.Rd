% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{save_ssn_div_hydrowindow_plots}
\alias{save_ssn_div_hydrowindow_plots}
\title{Save SSN hydro-window results (plots and tables)}
\usage{
save_ssn_div_hydrowindow_plots(
  perf_table,
  plot_varcomp = NULL,
  plot_obs_preds = NULL,
  plot_x_preds = NULL,
  plot_emmeans = NULL,
  plot_emtrends = NULL,
  in_organism,
  in_response_var,
  out_dir
)
}
\arguments{
\item{perf_table}{A \code{data.table} of performance results (from \code{\link[=prepare_hydrowindow_perf_table]{prepare_hydrowindow_perf_table()}}),
with the \code{mod} column removed.}

\item{plot_varcomp}{A list of \code{ggplot2} objects returned by \code{\link[=get_hydrowindow_varcomp]{get_hydrowindow_varcomp()}}.}

\item{plot_obs_preds}{A \code{ggplot2} object returned by \code{\link[=plot_hydrowindow_obs_preds]{plot_hydrowindow_obs_preds()}}.}

\item{plot_x_preds}{A \code{ggplot2} object returned by \code{\link[=plot_hydrowindow_x_preds]{plot_hydrowindow_x_preds()}}.}

\item{plot_emmeans}{A \code{ggplot2} object returned by \code{\link[=get_all_emmeans]{get_all_emmeans()}} (with \code{plot=TRUE}).}

\item{plot_emtrends}{A \code{ggplot2} object returned by \code{\link[=get_all_emtrends]{get_all_emtrends()}} (with \code{plot=TRUE}).}

\item{in_organism}{Character. Organism identifier.}

\item{in_response_var}{Character. Response variable identifier.}

\item{out_dir}{Character. Directory to save results.}
}
\value{
A named list of file paths to saved CSV and PNG files.
}
\description{
Saves performance tables and plots from the modular hydro-window pipeline to a specified directory.
}
\details{
\itemize{
\item The performance table is saved as CSV.
\item Variance decomposition plots (list of ggplots, one per page) are saved as multiple PNGs.
\item Other plots (\code{obs_preds}, \code{x_preds}, \code{emmeans}, \code{emtrends}) are saved as individual PNGs if present.
}
}
