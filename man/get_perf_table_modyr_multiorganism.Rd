% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{get_perf_table_modyr_multiorganism}
\alias{get_perf_table_modyr_multiorganism}
\title{Get performance table for models across multiple organisms}
\usage{
get_perf_table_modyr_multiorganism(in_mod_list)
}
\arguments{
\item{in_mod_list}{list. A named list of fitted model objects, with one model per organism.
Each model should be of class lm, glm, ssn_lm, ssn_glm, lme, or merMod.}
}
\value{
data.table. A combined data.table with one row per model containing:
\itemize{
\item \code{organism}: Organism identifier (from list names)
\item \code{formula}: Formatted model equation
\item \code{family}: Model family (for GLMs)
\item \code{tailup_type}: Tail-up covariance type (for SSN models)
\item \code{taildown_type}: Tail-down covariance type (for SSN models)
\item \code{euclid_type}: Euclidean covariance type (for SSN models)
\item Standard model metrics from \code{broom::glance}:
\itemize{
\item \code{r.squared}, \code{adj.r.squared}, \code{sigma}, \code{statistic}, \code{p.value}, \code{df}, \code{logLik},
\code{AIC}, \code{BIC}, \code{deviance}, \code{df.residual}, \code{nobs}
}
\item \code{GVIF}: Generalized Variance Inflation Factor (maximum across predictors)
\item \code{varcomp_country_only}: Variance explained by country fixed effect alone
\item \code{varcomp_hydroenv_covariates}: Variance explained by hydrological/environmental covariates
(fixed effects R² minus country-only R²)
\item Cross-validation metrics from \code{SSN2::loocv}:
\itemize{
\item \code{mape_loocv}: Mean Absolute Percentage Error
\item \code{pseudo_r2_loocv}: Pseudo R² from leave-one-out cross-validation
\item \code{cv_predict}: Cross-validated predictions
}
}
}
\description{
Extracts performance metrics from a list of fitted models
including variance decomposition, cross-validation, and goodness-of-fit statistics.
}
\details{
The function processes each model to extract:
\enumerate{
\item Model formula as a formatted equation
\item Variance decomposition from \code{SSN2::varcomp}
\item If country is a fixed effect, calculates:
\itemize{
\item Variance explained by country alone
\item Variance explained by other covariates (total fixed effects R² minus country-only R²)
}
\item Standard model metrics from \code{broom::glance}
\item Variance Inflation Factors from \code{car::vif}
\item Leave-one-out cross-validation metrics from \code{SSN2::loocv}
}
}
